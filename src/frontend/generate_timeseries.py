"""
Generate networks for time series analysis.
"""

import pandapower as pp
import pandas as pd
import tempfile


def generate_timeseries(net, ts_path):
    """
    Generate networks for time series analysis.

    Args:
        net: pandapower network.
        ts_path: path of time series data.

    Returns:
        nets: a list of pandapower network.
    """
    return 0

